#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-string-replace/bin/grunt-string-replace" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-string-replace/bin/grunt-string-replace" "$@"
  ret=$?
fi
exit $ret
